CREATE TABLE IF NOT EXISTS playerstats_24_25 (
    id SERIAL PRIMARY KEY,
    player VARCHAR(255),
    team VARCHAR(100),
    squad_number INTEGER,           
    nation VARCHAR(50),
    position VARCHAR(50),
    age VARCHAR(20),
    matches INTEGER,
    starts INTEGER,
    minutes INTEGER,
    goals INTEGER,
    assists INTEGER,
    penalty_shoot INTEGER,
    total_shoot INTEGER,
    shoot_on_target INTEGER,
    expected_goals_xg DECIMAL,
    non_penalty_xg_npxg DECIMAL,
    expected_assists_xag DECIMAL,   
    yellow_cards INTEGER,
    red_cards INTEGER,
    touches INTEGER,
    dribbles INTEGER,
    tackles INTEGER,
    blocks INTEGER,
    shot_creating_actions INTEGER,
    goal_creating_actions INTEGER,
    passes_completed INTEGER,
    passes_attempted INTEGER,
    progressive_passes INTEGER,
    carries INTEGER,
    progressive_carries INTEGER,
    dribble_attempts INTEGER,
    successful_dribbles INTEGER
);
